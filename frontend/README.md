# Pulse Frontend (Next.js + Tailwind)

This is a minimal Next.js + Tailwind frontend for the Pulse project.

Quick start (Windows PowerShell):

1. Install dependencies:

```powershell
cd frontend; npm install
```

2. Run the dev server:

```powershell
npm run dev
```

Open http://localhost:3000
