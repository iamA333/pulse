Pulse — FastAPI backend + Next.js + Tailwind frontend

This workspace contains two apps:

- `backend/` — FastAPI app (port 8000)
- `frontend/` — Next.js + Tailwind app (port 3000)

See `backend/README.md` and `frontend/README.md` for quick start instructions.
# pulse